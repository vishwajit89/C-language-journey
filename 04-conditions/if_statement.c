#include <stdio.h>
int main (){
	int age = 30 ;
	if (age >15){
		printf ("your age is greater then 15");
		printf ("we are inside if\n");
	}
if (age%4==2){ //renibder print hota ha 
		printf ("your age is greater then 15");
		printf ("we are inside if\n");
}
	return 0;
	}